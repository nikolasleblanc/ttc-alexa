service: aws-node-alexa-skill

frameworkVersion: ">=1.4.0 <2.0.0"

provider:
  name: aws
  runtime: nodejs4.3

functions:
  pop:
    handler: handler.ttc
    events:
      - alexaSkill

plugins:
  - serverless-webpack